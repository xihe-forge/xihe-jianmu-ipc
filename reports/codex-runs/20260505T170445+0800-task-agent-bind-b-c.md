# task-agent-bind B+C ship report

时间：2026-05-05T17:04:45+08:00  
执行：Codex  
ship-tier：e2e-full

## RCA

CC 内置 TaskCreate / TaskList 跟 Claude Code session id 绑定，同一 `IPC_NAME` 换 session id 后任务清单不可恢复。建木 Hub 已有按 agent 名持久化的 `tasks` 表和 `ipc_task` 能力，但 portfolio 默认任务流只写 CC 内置任务，没有透明双写到 Hub，也没有冷启动恢复 hint。

## 实现

### xihe-jianmu-ipc

- 为任务状态增加 `in_progress`，保留既有 `started` 兼容。
- 同步更新 MCP schema、协议常量、README、Hub API 测试与协议测试。
- 增加真 Hub 生命周期 e2e：同名 agent 重连后只恢复 `pending` / `in_progress`，不恢复 `completed`，并验证不同 agent 不串号。

### xihe-tianshu-harness

- 新增 `domains/software/hooks/task-agent-bind.ps1`：PostToolUse 拦截 `TaskCreate` / `TaskUpdate`，透明双写 Hub `/task` 与 `/tasks/:id`，`from=to=$IPC_NAME`，并维护 CC task id 到 Hub task id 映射文件。
- 新增 `domains/software/hooks/pending-tasks-hint.ps1`：SessionStart 查询 Hub `GET /tasks?agent=<ipc>`，过滤 terminal 状态，写入 `~/.claude/session-state/<ipc>.pending-tasks.json`。
- 更新 `templates/hooks-snippet.json` 注册链，并补齐 `tools/install-hooks.mjs` 覆盖测试期望。
- 更新 `domains/software/CLAUDE.md` 冷启动 SOP：读取 pending-tasks hint，并用 TaskCreate materialize 到当前 session。
- 已完成 5 件套同步：`handover/TODO.md`、`handover/PROJECT-PLAN.md`、`handover/HANDOVER-codex-task-agent-bind-20260505T1721.md`、`reports/cron-巡视/2026-05-05-task-agent-bind.md`、`C:/Users/jolen/.claude/projects/D--workspace-ai-research-xiheAi/memory/project_jianmu_ipc.md`。

## AC 真值

| AC | 结果 | 证据 |
|----|------|------|
| AC1 TaskCreate 后 Hub 可查且 title/description 一致 | PASS | `tests/task-agent-bind-ps1.test.mjs` TaskCreate 双写；Hub e2e 覆盖 create/list |
| AC2 TaskUpdate completed 后 Hub status 同步 | PASS | `tests/task-agent-bind-ps1.test.mjs` TaskUpdate PATCH 映射 |
| AC3 同名 agent 新 session 冷启动恢复 pending + in_progress，不恢复 completed | PASS | `tests/e2e/task-agent-rebind-lifecycle.test.mjs` 真 Hub + WebSocket 重连 |
| AC4 不同 ipc 名任务互不串号 | PASS | `tests/e2e/task-agent-rebind-lifecycle.test.mjs` foo/bar 隔离断言 |
| AC5 Hub 离线 / 超时不阻塞 CC 主流程 | PASS | hook 使用 fail-open `exit 0`，测试覆盖 stderr/log-only 行为 |
| AC6 单元测试覆盖 B hook 解析 / C materialize hint | PASS | `tests/task-agent-bind-ps1.test.mjs` |
| AC7 e2e lifecycle 真 spawn / reconnect / verify，不 mock | PASS | `tests/e2e/task-agent-rebind-lifecycle.test.mjs` 使用真实 Hub worker 与 WebSocket session |
| AC8 baseline 测试 | PASS_WITH_ENV_NOTE | harness full PASS；jianmu focused PASS；最终 `npm test` 被 unrelated untracked `tests/statusline-account.test.mjs` 阻断 |
| AC9 5 件套 + 跨仓 commit/push | PASS | harness 5 件套 commit 已在 `origin/main`；jianmu 本报告提交后推送 |

## 测试结果

- `xihe-tianshu-harness`: `node --test tests/*.mjs` PASS，50 tests。
- `xihe-jianmu-ipc` focused: `node --test tests/integration/task-in-progress-status.test.mjs tests/e2e/task-agent-rebind-lifecycle.test.mjs tests/mcp-tools.test.mjs tests/protocol.test.mjs` PASS。
- `xihe-jianmu-ipc` full: checkpoint 中 `npm test` PASS；最终复跑 `npm test` 被工作区已有 untracked `tests/statusline-account.test.mjs` 阻断，错误为找不到 `bin/statusline-account.mjs`。该文件不属于本任务提交，未修改、未删除。

## Commit 列表

### xihe-jianmu-ipc

- `ccd0556 test(task-agent): 覆盖 agent 绑定任务生命周期 RED`
- `47327a0 feat(task-agent): 支持 in_progress 任务状态`
- 本报告提交：`docs(report): task-agent-bind B+C ship 记录`

### xihe-tianshu-harness

- `a69ca7e test(task-agent): 覆盖 CC 任务双写和冷启 RED`
- `3608769 feat(task-agent): 双写 CC 任务并冷启恢复 hint`
- `1f63bcc docs(handover): 同步 task-agent-bind B+C 5 件套`

## Push status

- `xihe-tianshu-harness`: `main` 当前等于 `origin/main`，commit `1f63bcc` 已在远端。
- `xihe-jianmu-ipc`: `master` 当前领先 `origin/master` 3 个 commit；推送 `origin master`。

## 备注

- `xihe-jianmu-ipc` 存在 unrelated stash：`stash@{0}: On master: codex-preserve-unrelated-report`，未恢复、未删除。
- MCP `ipc_sessions` 在前序尝试中返回 `Transport closed`，完成通知将先重试 `ipc_send`，必要时记录 HTTP fallback。

EXIT 0
