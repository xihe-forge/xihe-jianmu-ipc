# idle-patrol spam emergency fix

## 修前 spam pattern

- xihe-ai 16:46 emergency：17min 内触发 4 次 idle-patrol spam。
- jianmu-pm 16:47 verify 真因：3min idle threshold + 3min patrol interval 过短；ack 后只有 L1 dedup，无 `source:topic` 全局持久 dedup；hold task 等外部 trigger 仍被 patrol。

## 修后行为

- `DEFAULT_IDLE_THRESHOLD_MS = 30 * 60 * 1000`。
- `DEFAULT_IDLE_PATROL_INTERVAL_MS = 30 * 60 * 1000`。
- `DEFAULT_ACK_DEDUP_MS = 60 * 60 * 1000`。
- IPC nudge 发送后按 `<sessionName>:idle-patrol` 写 `ack-dedup-state.json`，60min 内同 key 返回 `action='skip'`, `reason='ack-dedup'`。
- task description / subject / reason / activeForm / metadata 匹配 `hold`、`等`、`pending`、`blocked`、`waiting` 等 hold 词，或 `task.status == 'hold'`，或 `task.metadata.hold_reason` 非空时返回 `action='skip'`, `reason='hold-task'`。

## AC 真值

- AC1: PASS。默认 threshold / interval 均为 30min。
- AC2: PASS。默认 ack dedup 为 60min，持久化文件为 `ack-dedup-state.json`。
- AC3: PASS。新增 mock task description 含 `hold` / `等`，验证 skip `hold-task`。
- AC4: PASS。`node --test tests/watchdog-idle-patrol.test.mjs` -> 17 pass / 0 fail。
- AC5: PASS。`node --test` 最终复跑 -> 894 pass / 0 fail。
- AC6: PASS。dogfood live verify 非 dry-run：hold mock session 返回 `skip/hold-task` 且 IPC 0；非 hold 首次 nudge 后 30min 内同 session/topic 返回 `skip/ack-dedup`，总 IPC 1。

## Notes

- 未重启 daemon；仅 ship 代码。
- 全量 baseline 第一次遇到 transient PTY ordering + `127.0.0.1:31791` TIME_WAIT/EADDRINUSE，复跑通过。

EXIT 0
