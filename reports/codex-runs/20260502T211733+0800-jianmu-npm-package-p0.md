# jianmu-ipc npm package P0 ready-to-publish report

Task: task #22 P0 jianmu-ipc npm 包化 + MCP registry prep

## AC truth table

| AC | Result | Evidence |
|---|---:|---|
| AC1 package metadata | PASS | `package.json` name `@xihe-forge/jianmu-ipc`, `mcpName` `io.github.xihe-forge/jianmu-ipc`, `bin.jianmu-ipc`, repository/homepage/bugs/license, exports/main/files present. |
| AC2 README | PASS | Added `Quickstart for users`, feature bullets, and competitor comparison table. |
| AC3 MCP registry draft | PASS | Added `mcp-registry/server.json` with name/version/description/repository/npm package reference. |
| AC4 publish SOP | PASS | Added README `发布 SOP / Publishing SOP`; npm publish and `mcp-publisher publish` remain manual. |
| AC5 npm pack clean | PASS | `npm pack --dry-run` and `npm pack` report 48 files, 144.9 kB package, no `reports/`, `temp/`, `log/`, `logs/`, `handover/`, `.session-state`. |
| AC6 baseline | PASS | `node --test <all tests/**/*.test.mjs>`: tests 873, pass 873, fail 0. |
| AC7 dogfood live | PASS | Local tarball install, real hub start, MCP stdio `ipc_send`, `ipc_recent_messages` drain all passed. |

## npm pack

Command:

```powershell
npm pack --dry-run
npm pack
```

Tarball:

```text
filename: xihe-forge-jianmu-ipc-0.5.0.tgz
name: @xihe-forge/jianmu-ipc
version: 0.5.0
package size: 144.9 kB
unpacked size: 544.8 kB
total files: 48
```

File list:

```text
LICENSE
README.md
README.zh-CN.md
bin/codex-title-wrapper.mjs
bin/install.ps1
bin/jianmu.mjs
bin/README.md
hub.mjs
lib/agent-status.mjs
lib/audit.mjs
lib/ccusage-adapter.mjs
lib/channel-notification.mjs
lib/ci-relay.mjs
lib/claude-paths.mjs
lib/codex-app-server-client.mjs
lib/codex-thread-keepalive.mjs
lib/command-parser.mjs
lib/console-cards.mjs
lib/constants.mjs
lib/context-usage-auto-handover.mjs
lib/db.mjs
lib/feishu-adapter.mjs
lib/feishu-worker-thread.mjs
lib/harness-handover.mjs
lib/harness-heartbeat.mjs
lib/harness-state.mjs
lib/http-handlers.mjs
lib/internal-auth.mjs
lib/lineage.mjs
lib/mcp-tools.mjs
lib/network-events.mjs
lib/network-probes.mjs
lib/network-state.mjs
lib/observation-query.mjs
lib/openclaw-adapter.mjs
lib/protocol.mjs
lib/redact.mjs
lib/router.mjs
lib/session-names.mjs
lib/session-reclaim.mjs
lib/session-registry.mjs
lib/session-state-reader.mjs
lib/stuck-session-detector.mjs
lib/wake-cooldown.mjs
lib/zombie-pid-detector.mjs
mcp-server.mjs
mcp-wrapper.mjs
package.json
```

Forbidden path check: PASS, no `reports/`, `temp/`, `log/`, `logs/`, `handover/`, `.session-state`.

## Dogfood live output

Command:

```powershell
node tests/dogfood-npm-pack-live.mjs .\xihe-forge-jianmu-ipc-0.5.0.tgz
```

Output:

```json
{
  "ok": true,
  "tempRoot": "C:\\Users\\jolen\\AppData\\Local\\Temp\\jianmu-ipc-dogfood-CkKXZj",
  "package": "@xihe-forge/jianmu-ipc",
  "port": 47896,
  "senderName": "dogfood-sender-106116",
  "receiverName": "dogfood-receiver-106116",
  "sentContent": "dogfood-live-1777727910393",
  "recentCount": 1,
  "drainedMessageId": "msg_1777727916045_113c29"
}
```

Notes:

- The dogfood script installs the tarball into a temp directory with `--ignore-scripts`, then runs `npm rebuild better-sqlite3` so native dependency install scripts are still built without executing this package's PowerShell profile installer during verification.
- It starts the installed `bin/jianmu.mjs start`, starts the installed MCP stdio server via `bin/jianmu.mjs mcp`, calls `ipc_send`, then drains the message through `ipc_recent_messages`.

## Baseline

Command:

```powershell
$files = Get-ChildItem -Recurse -Path tests -Filter *.test.mjs | Sort-Object FullName | ForEach-Object { $_.FullName }; node --test $files
```

Result:

```text
tests 873
suites 30
pass 873
fail 0
cancelled 0
skipped 0
todo 0
duration_ms 34170.9096
```

## Manual publish checklist

Do not publish from automation. Maintainer must run:

```powershell
npm login
npm publish --access public
npm install -g mcp-publisher
mcp-publisher login github
mcp-publisher publish
npm view @xihe-forge/jianmu-ipc name version mcpName bin
```

Before publishing, re-run:

```powershell
npm pack --dry-run
npm pack
node tests/dogfood-npm-pack-live.mjs .\xihe-forge-jianmu-ipc-0.5.0.tgz
$files = Get-ChildItem -Recurse -Path tests -Filter *.test.mjs | Sort-Object FullName | ForEach-Object { $_.FullName }; node --test $files
```

## P1 out of scope

- Codex plugin manifest under `.codex-plugin/plugin.json`.
- Plugin skills/SOP bundle for user workflows.
- Plugin marketplace scanner/submit flow.

## Exit

EXIT 0
